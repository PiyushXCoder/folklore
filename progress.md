# Progress

## Done

- [x] Tauri + React + Vite + TS scaffold (default template)
- [x] `CLAUDE.md`, `progress.md`, `plans/` — project docs scaffolding
- [x] Deps: `superlore`, `fumadocs-core`, `fumadocs-ui`, `next-themes` (superlore's runtime peers), `fflate` (zip), `@tauri-apps/plugin-dialog`, `@tauri-apps/plugin-store`
- [x] Rust: `get_launch_path` (CLI-arg open) + `read_file_bytes` commands; dialog/store plugins registered; capabilities updated
- [x] Color scheme system (`src/theme/schemes.css`) — 7 schemes (Catppuccin Latte/Frappé/Macchiato/Mocha, Darcula, VS Code, Adwaita) + 14 accents as CSS vars on `<html data-scheme data-accent>`, global 2px radius override
- [x] `SUPERLORE_TOKENS` (`src/theme/tokens.ts`) — maps `SuperloreDoc`'s `tokens` prop onto `var(--app-*)`, so scheme/accent switches re-color the doc for free (no JS re-render)
- [x] `.superlore` bundle format + parser (`src/lib/superloreFile.ts`, zip via fflate: `doc.mdx` + `assets/` + `comments.json` + `meta.json`); plain `.mdx` passthrough
- [x] `rehypeResolveAssets` — rewrites `<img src="assets/...">` to blob URLs, passed via `SuperloreDoc`'s `rehypePlugins`
- [x] File acquisition: web picker/drag-drop, desktop dialog + native drag-drop + CLI-arg launch — all funnel through `openFile.ts`
- [x] `useSettings()` — scheme/accent, persisted via `localStorage` (web) / `@tauri-apps/plugin-store` (desktop)
- [x] `AppShell` layout: titlebar (with collapsible outline toggle) + `OutlineSidebar` (scanned from rendered `<h1-4>` DOM, not hand-parsed) + centered `Page` column wrapping `SuperloreDoc` (transparent bg, height fits content, no forced `min-height`)
- [x] `SettingsPanel` — scheme swatches (7) + accent grid (14)
- [x] Fixed white screen: superlore's Canvas component imports `next/dynamic.js` unconditionally (Canvas is lazy-loaded even in non-Next apps), which Vite treats as a missing optional peer dep and throws at runtime. Fixed with `vendor/next-shim` — a minimal local `next` package (`file:./vendor/next-shim` dependency) whose `dynamic.js`/`dynamic` exports are a `React.lazy` + `Suspense` equivalent, so Vite resolves it as a real installed package instead of special-casing it as missing.
- [x] Fixed doubled content-box artifact: `SuperloreDoc`'s `background` token now `transparent` (was `var(--app-bg)`), since `.page` alone should paint the surface
- [x] Verified: `tsc --noEmit` clean, `vite build` clean, `cargo check` clean, headless Playwright check (empty state renders; dropping a real `.mdx` renders content + outline sidebar picks up headings; all 7 schemes swap correctly; no console/page errors)
- [x] App icon/logo: `src/assets/accordion.svg` (svgrepo #396194) used as web favicon, titlebar + empty-state logo, and regenerated all Tauri desktop/iOS/Android icon sizes via `npx tauri icon`
- [x] Live-reload: doc view refreshes automatically when the source file changes on disk. Web via polling a `FileSystemFileHandle`'s `lastModified` (File System Access API — Chromium's `showOpenFilePicker` and `DataTransferItem.getAsFileSystemHandle()` on drag-drop; Firefox/Safari fall back to the old `<input>`/`DataTransfer.files` path with no live-reload). `src/lib/watchSource.ts` + `OpenedDoc.watch` in `src/lib/openFile.ts`.
- [x] Verified live-reload build: `tsc --noEmit` clean, `cargo check` clean, headless Playwright check of both the plain-`<input>` path and a simulated drag-drop (including the null-handle fallback case, which surfaced and got fixed — a `getAsFileSystemHandle()` resolving to `null` was throwing instead of falling back to the plain `File`)
- [x] **Fixed: desktop live-reload wasn't firing at all.** First attempt used `@tauri-apps/plugin-fs`'s `watch()` gated by a `fs:scope: ["**"]` capability — that promise silently never resolved/fired and the frontend had no `.catch`, so nothing surfaced. Replaced with a custom Rust implementation: `watch_path`/`unwatch_path` commands built directly on the `notify` crate (like `read_file_bytes`, a plain command bypasses the fs-plugin ACL instead of fighting its scope glob syntax), emitting a per-watch `file-changed:<id>` Tauri event the frontend listens for and debounces (200ms — a single save fires multiple inotify events). Removed `@tauri-apps/plugin-fs` and the `fs:*` capability entries entirely. Verified the `notify` watch logic in isolation with a standalone Rust smoke test (write → Modify event received) before wiring it back into the app.
- [x] **Fixed: live-reload only worked "sometimes."** Root cause: `watch_path` watched the file's own path directly. Editors that save atomically (write a temp file + `rename()` over the original — vim, VS Code, many others) replace the inode on save; a watch on the file's own path goes dead after the *first* such save, so plain in-place writes kept working but atomic-save editors broke after edit #1. Confirmed with a standalone test: 0 events reached a file-direct watcher on the second atomic save. Fixed by watching the **parent directory** instead and filtering events by filename (`is_modify() || is_create() || is_remove()`, excluding `Access` events specifically so the reload's own file read doesn't retrigger itself) — verified both saves now fire correctly.
- [x] **Fixed: Canvas node shapes ("charts") not drawing right.** Our global 2px-radius mandate (`* { border-radius: 2px !important }`) was clobbering superlore Canvas's node shapes — a `circle` node rendered as a square, `rounded`/pill nodes lost their curve. First fix attempt (`.react-flow * { border-radius: revert !important }`) silently didn't work: `revert` resets to the *user-agent* default (`0`), not to whatever author rule set the real shape radius, so it looked identical to the bug. Actual fix: exclude Canvas's shape-surface class directly at the source — `*:not(.kp-canvas-node)` in the global rule (`.kp-canvas-node` is the `SURFACE` const superlore's `nodes.mjs` puts on every shape). Verified circle/diamond/rounded node kinds render correctly now, with the flat-2px chrome elsewhere (`.page`, buttons) unaffected.
- [x] **Fixed: Canvas edges/arrows invisible in the real desktop app** (not reproducible in headless Chromium, which is all we'd verified Canvas against up to this point). Set up a real `tauri dev`-equivalent under Xvfb + WebKitGTK to test against Tauri's actual engine, and confirmed a genuine WebKitGTK-only bug: `@xyflow/react`'s own base CSS (`.react-flow .react-flow__edges svg { overflow: visible }`) never made it into superlore's compiled bundle, and WebKitGTK — unlike Chromium — force-clips the outermost `<svg>` to its own box regardless of the `overflow` property (confirmed live: computed style showed `overflow: visible`, edges still clipped). Fixed by giving the edges `<svg>` real fixed pixel dimensions (`8000px` square) so there's no box edge left to clip at. Verified against the exact multi-node/frame/routed-edge diagram from the original bug report, rendering correctly end-to-end in a real Tauri window.
- [x] **Process note**: all Canvas rendering must be checked in an actual Tauri window (or WebKit) going forward — headless Chromium/Playwright cannot catch this whole class of WebKitGTK-specific bug and had given false confidence on the two prior Canvas fixes above.

## Next

- [ ] `pnpm tauri dev` smoke test — user to confirm live-reload fires consistently and Canvas arrows render correctly in their actual (non-Xvfb) desktop environment
- [ ] OS file-association registration (`tauri.conf.json` `bundle.fileAssociations`) so double-clicking `.superlore`/`.mdx` launches folklore directly, not just `folklore <path>` from a terminal
- [ ] Comments UI for `.superlore` bundles (parsed into `bundle.comments`, not yet rendered)
- [ ] Single-instance handling (`tauri-plugin-single-instance`) so a second `folklore <path>` launch reuses the open window instead of spawning a new one

## Plans

See `plans/0001-viewer-foundation.md` for the foundational architecture plan.
